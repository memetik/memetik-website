{
  "buildCommand": "node scripts/build-articles.cjs && node scripts/build-sitemap.cjs && node scripts/build-llms-txt.cjs && npm run build:client",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
